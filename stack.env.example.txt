# Vorlage fuer die Portainer-Stack-Umgebungsvariablen.
# In Portainer NICHT als Datei ins Repo, sondern im Stack-Editor unter
# "Environment variables" eintragen, oder als .env neben der Stack-Datei
# auf dem Host mit chmod 600 ablegen.

# Pflicht: langer, zufaelliger API-Key fuer den Zugriff auf /analyze.
# Erzeugen z. B. mit: openssl rand -hex 32
TRADINGAGENTS_API_KEY=

# Kommagetrennte Ticker-Allowlist. Leer lassen = keine Einschraenkung
# (nicht empfohlen fuer den produktiven Anfang).
TRADINGAGENTS_TICKER_ALLOWLIST=AAPL,MSFT,NVDA,BTC-USD,ETH-USD

# Max. gleichzeitige Analysen (LLM-Kosten/Last begrenzen).
TRADINGAGENTS_MAX_CONCURRENCY=1

# LLM-Provider und Modelle.
TRADINGAGENTS_LLM_PROVIDER=openai
TRADINGAGENTS_DEEP_THINK_LLM=gpt-5.5
TRADINGAGENTS_QUICK_THINK_LLM=gpt-5.4-mini
TRADINGAGENTS_MAX_DEBATE_ROUNDS=1
TRADINGAGENTS_MAX_RISK_ROUNDS=1
TRADINGAGENTS_TEMPERATURE=0
TRADINGAGENTS_CHECKPOINT_ENABLED=true

# Nur den Key des tatsaechlich genutzten Providers befuellen.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
ALPHA_VANTAGE_API_KEY=

LOG_LEVEL=INFO
